window.RichManAI={shouldBuy(player,tile){if(!tile||tile.price<=0)return false;const reserve=350;return player.cash-tile.price>reserve||tile.price<280},shouldUpgrade(player,tile,cost){return tile&&tile.lv<3&&player.cash-cost>500},chooseCard(player){if(player.cards&&player.cards.bonus>0&&player.cash<500)return 'bonus';return null},chooseStock(player,stocks){let best=0;stocks.forEach((s,i)=>{if(s.p<stocks[best].p)best=i});return player.cash>900?best:null}};